<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>MVC Tech Blog</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
        <link rel="stylesheet" href="./Assets/css/style.css" />
    </head>

    <body class="body">
        <header class="header row">
            {{#if loggedIn}}
            <div class="two columns">
                <a href="/" class="button hbutton">Home</a>
                <a href="/profile" class="button hbutton">My Profile</a>
            </div>
            {{else}}
            <div class="two columns">
                <a href="/login" class="button hbutton login-button" id="loginBtn">Login</a>
            </div>
            {{/if}}        
        </header>
<!-- this is where handlebars will change stuff -->
        <main class="row main">
            {{{body}}}
        </main>
        {{#if loggedIn}}
        <script src="./Assets/js/script.js"></script>
        {{/if}}
    </body>
</html>
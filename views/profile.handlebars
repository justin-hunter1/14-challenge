    <div class="mb-5 pt-3">
        {{!-- <a href="/profile/create/"><button id="create-post" class="button">create post</button></a> --}}
        
        <h2 class="title pb-4">dashboard: your posts</h2><hr>
{{#if user}}
    </div>

    {{#each user as |user|}}
        {{> blog-data2 user}}
        
            <a href="/profile/edit/{{post.id}}"><button class="button">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">you do not currently have any posts</h6>

{{/if}}


